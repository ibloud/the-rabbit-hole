import fs from 'node:fs';const ids=['seven-sins','sick-boi','money-game-pt3'];for(const id of ids){const r=JSON.parse(fs.readFileSync(`content/rooms/${id}.json`));if(!r.title||!r.hotspots.length)throw new Error(`Smoke test failed: ${id}`);for(const h of r.hotspots)if(!h.accessibilityLabel)throw new Error(`Accessibility label missing: ${id}/${h.id}`)}console.log('Runtime smoke test passed.');
