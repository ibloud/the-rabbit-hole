import fs from 'node:fs';const dir='content/rooms';for(const file of fs.readdirSync(dir).filter(f=>f.endsWith('.json'))){const r=JSON.parse(fs.readFileSync(`${dir}/${file}`,'utf8'));for(const k of ['id','title','track','description','hotspots'])if(!(k in r))throw new Error(`${file}: missing ${k}`);if(!Array.isArray(r.hotspots))throw new Error(`${file}: hotspots must be an array`);for(const h of r.hotspots)for(const k of ['id','label','type','dialogue','accessibilityLabel'])if(!(k in h))throw new Error(`${file}: hotspot missing ${k}`)}console.log('Content validation passed.');
